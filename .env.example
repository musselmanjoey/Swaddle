# Spotify Configuration
# Copy this file to .env.local and fill in your actual values
# .env.local is ignored by git, so your secrets stay private

REACT_APP_SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# Genius API Configuration (for lyrics analysis)
REACT_APP_GENIUS_ACCESS_TOKEN=your_genius_access_token_here

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=swaddle
DB_USER=postgres
DB_PASSWORD=your_database_password_here

# Instructions:
# 1. Copy this file: cp .env.example .env.local
# 2. Edit .env.local with your real values
# 3. Install PostgreSQL locally
# 4. Create a database called 'swaddle'
# 5. Never commit .env.local to git!
